# Download and install 'Git'   #  https://git-scm.com/install/windows
#           git --version
# Open Github url and login to your Github Account - https://github.com/dashboard
# Git Initilization - Remember your Username and Email id
# Goto Command Prompt(Local) - (Connection between local Git and Github Repository.)
    # git config --global user.name "mkdash2006"
    # git config --global user.email "mkdash2006@gmail.com"
# To check config details
# git config --global --list
   # OR
#   git config --global user.name
#   git config --global user.email
# To Remove github Account
#   git config --global --unset user.name
#   git config --global --unset user.email
# one install - in the project path terminal
   # …or create a new repository on the command line
echo "# playwrightPython_MyProject_Main1" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/mkdash2006/playwrightPython_MyProject_Main1.git
git push -u origin main
…or push an existing repository from the command line
git remote add origin https://github.com/mkdash2006/playwrightPython_MyProject_Main1.git
git branch -M main
git push -u origin main


##########
Once all file copied to repository
Git Clone - copy the https url from github, then goto
your local folder cmd and type
git clone url
########### CHILD BRANCH
In Github, create a child Branch 'ex- dataHandling'. (search and add child branch name)
Then automatically. all the code from main are copied to child branch
---------------------------
Tester suppose to puch code change to branch not to main
So to get the child branch code in my local vs code, type
   git pull    ##copy child branch to my local)
   Then perform always the push in child branch 
         git add .
         git commit -m "comment"
         git push
   


######### after each code change
git add .
git commit -m "comment"
git push
